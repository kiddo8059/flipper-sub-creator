#pragma once

#include <furi.h>
#include <furi_hal.h>

typedef enum {
    SubCreatorViewSubmenu,
    SubCreatorViewPopup,
    SubCreatorViewByteInput,
    SubCreatorViewTextInput,
    SubCreatorViewWidget,
} SubCreatorView;
